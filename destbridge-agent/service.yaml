apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: agent
    app.kubernetes.io/name: destbridge-agent
  name: destbridge-agent
  namespace: default
spec:
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
      nodePort: 30010
  selector:
    app.kubernetes.io/instance: agent
    app.kubernetes.io/name: destbridge-agent
  type: NodePort

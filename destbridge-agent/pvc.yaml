apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: destbridge-agent
spec:
    accessModes:
    - ReadWriteOnce
    resources:
      requests:
        storage: 1Gi
    storageClassName: local-path
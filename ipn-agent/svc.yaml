apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: agent
    app.kubernetes.io/name: ipn-agent
  name: ipn-agent
  namespace: default
spec:
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
      nodePort: 30011
  selector:
    app.kubernetes.io/instance: agent
    app.kubernetes.io/name: ipn-agent
  type: NodePort
